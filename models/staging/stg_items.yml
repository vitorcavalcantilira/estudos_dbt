models:
  - name: stg_items
    description: Staging table for order line items, containing individual products within orders.
    columns:
      - name: id
        description: Unique identifier for each item
      - name: order_id
        description: Identifier of the order associated with the item
      - name: sku
        description: Stock Keeping Unit code for the item
